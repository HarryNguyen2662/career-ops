# 024 — Artie — Software Engineer

**Date:** 2026-05-29
**Score:** 3.6/5
**URL:** https://jobs.ashbyhq.com/artie/9a743ac5-2228-4522-895f-139dac5426a2
**PDF:** ✅
**Legitimacy:** High Confidence

---

## Machine Summary

```yaml
company: Artie
role: Software Engineer
date: 2026-05-29
score: 3.6
status: Evaluated
level: Mid-level implied ($150K–$300K range, small team, "multiple hats") — open to strong juniors
location: San Francisco — on-site 5 days/week, Financial District
comp_base: "$150,000–$300,000 USD"
comp_equity: "Competitive equity — YC + General Catalyst backed; illiquid pre-exit"
archetype: "Backend / Distributed Systems Engineer (Data Infrastructure)"
graduation_timing_risk: low — no graduation restriction stated; May 2027 start fine
visa_risk: low — "Visa sponsorship available" explicitly stated in JD; YC-backed startup (verify LCA history)
apply_rec: "Conditional — 3.6/5 below 4.0 threshold; CDC domain gap is meaningful; strong distributed systems match on hard requirements; worth applying if Harry is interested in data infra/streaming as a domain direction"
```

---

## Block A — Role Summary

| Dimension | Detail |
|---|---|
| **Archetype** | Backend / Distributed Systems Engineer — Data Infrastructure |
| **Domain** | Real-time CDC (Change Data Capture) streaming; zero-latency data replication |
| **Function** | Build CDC connectors, pipeline optimizations, encryption features, infra tooling; customer-facing exposure |
| **Seniority** | Not stated — $150K–$300K range implies 2-5 years; small team suggests strong juniors considered |
| **Location** | San Francisco — on-site 5 days/week (Financial District) |
| **Stack** | Go (preferred), TypeScript/React, Postgres, Redis, Kafka, Elasticsearch, Terraform, Kubernetes/Helm, GCP + AWS |
| **Comp** | $150K–$300K base + competitive equity + healthcare/401k/PTO + meals |
| **Visa** | Explicitly stated: "Visa sponsorship available" |
| **TL;DR** | Artie needs a distributed systems engineer to build their CDC platform: connectors, pipeline optimization, infra tooling. Go preferred, Kafka/K8s preferred. All preferred skills soft. Hard requirements are CS fundamentals + pragmatism + multi-hat comfort. |

---

## Block B — Match with CV

**Score: 3.5/5**

| JD Requirement | Harry's Evidence | Fit |
|---|---|---|
| **Strong CS fundamentals** | Distributed Systems, OS, DB, Networks coursework; Chrome production C++; lock-free trie + Protobuf IPC | ✅ Strong |
| **gRPC (preferred)** | TiMoto: production gRPC — deadlock traced + fixed under concurrent load; sub-50ms p99 | ✅ Direct |
| **Exactly-once / async systems** | CoderPush: idempotent APIs with Redis (85% hit rate) + exponential backoff; TiMoto: circuit breakers, exactly-once semantics | ✅ Strong |
| **Redis** | CoderPush: Redis distributed caching, 85% cache hit rate | ✅ Direct |
| **Terraform / IaC** | TiMoto: multi-AZ ECS Fargate with Terraform IaC, auto-rollback on health check | ✅ Direct |
| **TypeScript/React** | Chrome: TypeScript/React observer system, 25K+ lines, 95% coverage | ✅ Strong |
| **Postgres** | Develop for Good: N+1 fix + indexing → sub-100ms on 10K+ records | ✅ Application-level |
| **AWS / GCP** | TiMoto: ECS Fargate, EC2, S3, RDS; Develop for Good AWS BaaS; GCP in skills | ✅ |
| **Memory/disk tradeoffs** | TiMoto: PagedAttention for KV cache fragmentation under concurrent load; Chrome: mutex elimination via lock-free trie | ✅ Adjacent |
| **Wearing multiple hats** | TiMoto: sole engineer — inference, backend REST, gRPC, AWS, CI/CD, monitoring | ✅ Direct |
| **Go (preferred)** | Pulumi: Go CLI contributions and bug fixes — OSS only, not production backend | ⚠️ OSS; not backend |
| **Kafka (preferred)** | Listed in skills; no demonstrated production experience | ⚠️ Listed only |
| **Kubernetes/Helm (preferred)** | Docker + K8s in skills; primary infra is ECS Fargate; not K8s-native production | ⚠️ Adjacent |
| **CDC / stream processing** | No CDC experience — domain-specific gap; core Artie product | ❌ Missing |
| **SF on-site 5 days/week** | Atlanta; open to relocation | ⚠️ Relocation required |
| **Degree completion** | Expected May 2027 | ⚠️ In progress |

**Key strength:** All hard requirements met — CS fundamentals, gRPC production depth, distributed systems correctness (exactly-once, circuit breakers), Terraform, "multiple hats" TiMoto solo ownership exactly matches Artie's startup culture.

**Key gap:** CDC is the core of Artie's product — no CDC/stream processing experience. Go is preferred primary backend language; Harry has OSS contributions only (Pulumi). Kafka not demonstrated.

---

## Block C — Level and Strategy

JD does not state seniority explicitly. "$150K–$300K" is a wide band suggesting slots from 2yr to 6yr. Small team + "multiple hats" suggests they will consider strong fundamentals over domain checklist — the "preferred, not required" language on Go/Kafka/K8s explicitly signals this.

**Sell-strong plan:**
- Lead with gRPC production experience (exact preferred-tech match)
- TiMoto sole engineer = Artie's cultural archetype — builds 0→1, owns all layers
- Distributed systems correctness depth (exactly-once, circuit breakers, deadlock diagnosis)
- Terraform + AWS + Redis = direct stack overlap

**Domain gap strategy:** CDC is learnable. The engineering challenges (exactly-once, high-throughput partitioned streams, connector patterns) are within Harry's existing mental model from CoderPush + TiMoto. Lean on: "I've solved the same class of problem (high-throughput, exactly-once, async) at different layers."

**Recommendation: Conditional — below threshold.** Worth applying if data infrastructure / streaming is a domain Harry wants to develop. If focused on ML infra or pure systems roles, time better spent on pipeline 4.0+ roles.

---

## Block D — Comp and Demand

**Score: 4.0/5**

| Component | Value | vs Target |
|---|---|---|
| Base | **$150,000–$300,000** | **Floor at walk-away ($140K); within target range ✅** |
| Equity | Competitive — YC + General Catalyst + Pathlight Ventures backed | Illiquid pre-exit; upside meaningful if Artie scales |
| Benefits | Healthcare, 401k, unlimited PTO, lunch + dinner provided | Above-standard for startup |

**Year-1 TC estimate: ~$160K–$195K** (new-grad/junior band + equity vesting)

ClickUp, Substack, Alloy as named customers. YC + GC = well-capitalized growth-stage startup. Equity upside credible relative to pure seed-stage risk.

---

## Block E — Customization Plan

| # | Section | Change | Why |
|---|---|---|---|
| 1 | TiMoto framing | "Founding Engineer — Real-Time Data Platform" | Maps to "multiple hats" + sole engineer culture |
| 2 | TiMoto bullets | Lead with gRPC production + exactly-once semantics; include Terraform/circuit breaker | gRPC is preferred tech; exactly-once = core CDC correctness requirement |
| 3 | CoderPush bullets | Lead with "high-throughput data pipeline at 9,000+ req/sec" + partition redesign | Artie vocabulary: data pipelines, throughput, partition strategy |
| 4 | Pulumi heading | Add "Go" explicitly to tech tags | Go is preferred; Pulumi in Go is the only Go signal |
| 5 | Languages | Go elevated to position 2 after TypeScript | Preferred language for Artie; TypeScript also in stack |
| 6 | Skills | Lead with "Distributed Systems & Data Infrastructure" — gRPC, Kafka, Redis, Exactly-once | Direct Artie stack vocabulary |
| 7 | Skills | Add Kafka, Kubernetes, Elasticsearch explicitly | All in Artie tech stack |

---

## Block F — Interview Plan

Artie interviews for: distributed systems fundamentals, pragmatic problem-solving, customer communication, multi-hat comfort.

| # | Theme | Story |
|---|---|---|
| 1 | Production gRPC / concurrent systems | TiMoto: gRPC deadlock traced to circular resource acquisition; redesigned call sequencing → 100% success at sub-50ms p99 |
| 2 | Memory/disk tradeoffs | TiMoto: PagedAttention selected for KV cache fragmentation under concurrent agent load — virtual memory efficiency without page faults |
| 3 | High-throughput data pipelines | CoderPush: DynamoDB hot-partition at 9K+ req/sec — access pattern analysis → partition redesign → 30% throughput improvement |
| 4 | Exactly-once semantics | CoderPush: idempotent APIs + Redis (85% hit rate) + exponential backoff → exactly-once under network partitions |
| 5 | Infra + reliability | TiMoto: multi-AZ ECS + circuit breaker + Terraform → 99.9% uptime, 44% cost reduction; auto-rollback on health failure |
| 6 | Performance profiling | Chrome: p99 bottleneck at 1,200ms → lock-free trie → 96% latency reduction, zero regressions |
| 7 | Wearing multiple hats | TiMoto: sole engineer across vLLM inference, Django REST backend, gRPC layer, AWS infra, CI/CD, monitoring |

**Case study:** TiMoto end-to-end — "I built and operate a production system across every layer; at Artie that's exactly the operating mode."

**CDC knowledge gap:** "I haven't built CDC connectors yet, but the problems I've solved — exactly-once delivery, high-throughput partition strategy, fault-tolerant pipelines — are the same class of correctness challenges. I'd ramp on the domain-specific patterns quickly."

---

## Block G — Posting Legitimacy

**Tier: High Confidence**

| Signal | Finding | Weight |
|---|---|---|
| Posting active | Live Ashby form; full JD with comp range and benefits detail | Positive |
| Salary transparency | $150K–$300K explicitly stated | Positive |
| Company verification | Artie — YC, General Catalyst, Pathlight Ventures backed; customers ClickUp, Substack, Alloy named | Positive |
| JD specificity | Named technologies (Go, Kafka, CDC, K8s/Helm, Terraform); specific day-to-day tasks listed | Positive |
| Visa sponsorship | Explicitly stated: "Visa sponsorship available" | Positive |
| H-1B LCA history | Small startup — LCA history unverified; explicit statement is positive signal | Neutral (verify with recruiter) |

---

## Global Score

**3.6/5 — Conditional**

| Dimension | Score | Driver |
|---|---|---|
| CV Match | 3.5 | gRPC + exactly-once + Terraform + Redis direct match; CDC domain gap; Go OSS only |
| North Star | 3.8 | Backend/Distributed Systems is primary archetype; data streaming adjacent |
| Compensation | 4.0 | $150K floor at walk-away minimum; wide ceiling; credible equity |
| Culture | 4.0 | Explicit visa sponsorship; small founding-engineer culture = TiMoto archetype; SF onsite from Atlanta |
| Red flag adj. | -0.7 | CDC domain gap (-0.3); Go OSS only not production (-0.15); relocation SF (-0.1); degree in progress (-0.1); Kafka not demonstrated (-0.05) |
| **Global** | **3.6** | |

**Recommendation: Conditional.** Below 4.0 threshold. Strongest case for applying: explicit visa sponsorship + distributed systems primary archetype + TiMoto sole-engineer culture fit + comp floor at target. Main risk: CDC is the core product — a domain Harry hasn't touched. Apply if data infrastructure/streaming is a direction Harry wants to develop; skip if focused on ML infra or pure systems roles.
