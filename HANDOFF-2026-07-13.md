# Handoff Report — career-ops session (2026-07-13)

Báo cáo bàn giao cho phiên Claude Code tiếp theo. Đọc file này trước khi tiếp tục. PR #9 đã merge vào `main` trên fork của Harry (`HarryNguyen2662/career-ops`) — mọi thứ dưới đây đã ở trên `main`, không có branch riêng nào đang chờ merge.

---

## 0. Việc quan trọng nhất cần biết trước khi làm bất cứ gì

**LUÔN LÀM VIỆC TRONG MAIN WORKTREE, KHÔNG PHẢI SESSION WORKTREE.**

Phiên này chạy trong một git worktree session (`.claude/worktrees/{tên-ngẫu-nhiên}/`), nhưng **toàn bộ dữ liệu thật (portals.yml, data/, reports/, output/, cv.md, config/) chỉ tồn tại đầy đủ và chính xác ở main worktree**: `/Users/toannguyen/career-ops`. Session worktree là một checkout git riêng biệt — nó CÓ THỂ thiếu file (untracked/gitignored data không tự động copy qua), và bất kỳ thay đổi nào làm trong đó sẽ KHÔNG xuất hiện ở main worktree.

**Quy tắc bắt buộc:** Mọi lệnh Bash liên quan đến project phải prefix bằng `cd /Users/toannguyen/career-ops &&` — vì shell cwd reset về session worktree sau MỖI lệnh (đây là hành vi cố ý của harness, không phải bug). Đã mất rất nhiều thời gian trong phiên này để phát hiện ra điều này — đừng lặp lại.

Các tool khác cũng bị ảnh hưởng bởi ranh giới worktree này:
- **Preview/launch.json**: tool `preview_start` đọc `.claude/launch.json` từ SESSION worktree, không phải main. Nếu cần chạy dev server (VD: `web/`), phải tạo `launch.json` trong session worktree nhưng trỏ đường dẫn tuyệt đối vào main worktree (VD: `bash -c "cd /Users/toannguyen/career-ops/web && CAREER_OPS_ROOT=/Users/toannguyen/career-ops npm run dev"`).
- **pdf-viewer (display_pdf)**: chỉ cho phép đọc file trong session worktree ("Local file not in allowed list"). Muốn xem PDF từ main worktree, phải `cp` nó vào session worktree trước (VD: `.claude/worktrees/{tên}/output/...`).

---

## 1. Việc đã hoàn thành trong phiên này (tóm tắt theo commit, xem `git log` để chi tiết)

### 1a. Sửa lỗi hệ thống v1.18.0 (3 commit lớn, ~78 file)
`update-system.mjs` (cả upstream lẫn bản local) có một lỗ hổng từ trước: nó liệt kê đúng các file trong `SYSTEM_PATHS` nhưng **không thực sự đồng bộ hết** — khiến checkout này thiếu 78 file, bao gồm những cái CỰC KỲ quan trọng: `set-status.mjs` (script chuẩn để update tracker), `application-answers.mjs`, `prepare-application.mjs`, `plugins/_engine.mjs` (+4 file), `verify-portals.mjs`, `fingerprint-core.mjs`, `tracker-parse.mjs`, `tracker-utils.mjs`, `role-matcher.mjs`, và toàn bộ mode file như `modes/email.md`, `modes/offer-prep.md`, `modes/agent-inbox.md`. Đã trace toàn bộ import chain từ `FETCH_HEAD` và kéo về hết. **Nếu gặp lỗi `ERR_MODULE_NOT_FOUND` khi chạy bất kỳ script `.mjs` nào, kiểm tra xem file đó đã tồn tại chưa trước khi nghi ngờ code logic.**

Cũng đã **vá `update-system.mjs`** để không lặp lại lỗi này:
- Thêm pre-flight guard: từ chối chạy `apply()` nếu có file user-layer đang dirty (trước đây, điều này khiến SAFETY VIOLATION rollback xóa mất file hệ thống mới thay vì chỉ revert đúng file user).
- Sửa bug `.trim()` → `.trimEnd()` trong hàm `git()` dùng chung — `.trim()` cắt mất khoảng trắng đầu dòng của dòng ĐẦU TIÊN trong `git status --porcelain` nếu dòng đó có code bắt đầu bằng space (VD: " M portals.yml"), làm hỏng việc parse path.

### 1b. Tracker rebuild
`data/applications.md` trước đây chỉ có **1 dòng** dù có **155 report** trong `reports/` — phiên đánh giá gốc (27/5–14/6) không bao giờ chạy `merge-tracker.mjs` trước khi kết thúc. Đã resolve 3 vụ trùng số report (Glean 001→154, Everpure 012→155, Glean Billing 026→156), parse header của cả 155 report, và merge đúng quy trình. Giờ tracker có **155 dòng** (154 Evaluated + 1 Applied — Harper #147).

**Cảnh báo còn tồn đọng (không phải lỗi, chỉ là lưu ý):** `verify-pipeline.mjs` báo 11 warning — 6 cặp company+role bị đánh giá lại 2 lần ở ngày khác nhau (Amazon SDE #013/#117, Notion #056/#149, Plaid #042/#91/#100, IMC #21/#57, Meta #39/#70, Glean #79/#154, Google YouTube Ads #127/#152) được giữ thành dòng riêng thay vì gộp, theo đúng logic fuzzy-match sẵn có của `merge-tracker.mjs` (không phải bug tao gây ra).

### 1c. Portal scanning (4 round, xem `data/scan-history.tsv`)
`data/pipeline.md` hiện có **985 offer đang chờ đánh giá** (tăng từ 692 lúc đầu phiên). Đã sửa 5 `careers_url` hỏng trong `portals.yml` (Qualcomm, Bank of America, American Express, Benchling → Ashby, IBM → cần `provider: ibm` tường minh). Đã document các blocker chưa giải được: Amazon (SPA không render qua Playwright — nhưng giờ đã có `providers/amazon.mjs` zero-token, không cần Playwright nữa), Apple, Atlassian, OpenAI (700+ listing không tag New Grad), Palo Alto Networks (route qua RippleMatch ngoài), Unity Technologies (API 404 dù board vẫn sống — cần Playwright thay vì API).

**Còn ~40 công ty chưa quét bằng Playwright** (Samsung, SAP, Secureframe, Sentry, ServiceNow, Shopify, Slack, Snowflake, Snyk, Sony, SpaceX, Square, SurveyMonkey, Tempus Labs, Tesla, TuSimple, Twilio, Two Sigma, Uber, Upgrade, Veeva, Verkada, Vice Media, VMware, Walmart, Wayfair, Waymo, Wealthfront, WeWork, Wish, Workato, Workday, X, Yahoo, Yelp, Yext, ZipRecruiter, Zoom — Rippling và Salesforce đã quét 1 phần, còn nhiều trang chưa duyệt hết vì board quá lớn 500+/700+ role).

### 1d. career-ops web v0.3.0
Đã pull `web/` (không được `update-system.mjs` đồng bộ tự động — là tính năng opt-in). Đã verify chạy thật, đọc đúng data (923+ pending, đúng số Evaluated/Applied). Cần `CAREER_OPS_ROOT=/Users/toannguyen/career-ops` khi chạy `npm run dev` từ session worktree (xem mục 0).

### 1e. CV rewrite toàn diện
Viết lại TOÀN BỘ bullet trong `cv.md` theo kỹ thuật impact-first (XYZ formula: "Accomplished X, as measured by Y, by doing Z" — Laszlo Bock/Google), cộng thêm: replacing-X-with-Y framing, downstream/second-order impact (có số thật, verify với TiMoto leadership — 68% MTTI reduction), "preventing X" framing, end-user-facing framing. Không bịa số nào — mọi số liệu đều lấy từ `cv.md`/`interview-prep/story-bank.md` gốc hoặc do Harry verify trực tiếp trong hội thoại.

**Đã sửa 1 bug thật:** `templates/cv-template.tex` + `build-cv-latex.mjs` cùng thêm prefix `mailto:` → tạo ra `mailto:mailto:email@...` (link email hỏng) trên MỌI CV LaTeX sinh từ template này trước đây. Đã sửa — LinkedIn/GitHub không bị vì chỉ có 1 nơi thêm scheme.

### 1f. Report mới
**#157 — Google, Software Engineer II Early Career, AI Career Catalyst Program** (Sunnyvale, CA). Score 4.3/5. Rotational program AI+Infrastructure, comp $123-175K + 15% bonus + equity, start June 2027 khớp chính xác với tốt nghiệp May 2027. Đã regenerate PDF sau khi CV rewrite (2 lần — lần đầu quên chưa fix bug email, lần 2 đã fix).

---

## 2. Việc CHƯA hoàn thành — cần làm ở phiên tiếp theo

### 2a. 985 offer trong pipeline chưa được đánh giá
Đây là backlog lớn nhất hiện tại. Khuyến nghị: dùng `career-ops web` (Pipeline → Explore tab) để filter miễn phí (freshness/source/seniority/location) trước khi tốn token đánh giá batch — đây chính là tính năng "inbox triage" mà v1.17.0/v1.18.0 giới thiệu.

### 2b. Các HANDOFF item cũ từ 2026-06-14 (vẫn còn treo, xem `git log -- HANDOFF-2026-06-14.md` nếu file đã bị xóa)
- **Twilio #151** — form đã điền, chờ Harry trả lời 5 câu EEO
- **Roblox #153** — form đã điền, chờ quyết định submit (JD nói không hỗ trợ H-1B)
- **Notion #149** — form bị bỏ dở, cần làm lại từ đầu
- **Harper #147** — đã tự nộp, cần xác nhận H-1B trước khi họ phản hồi
- **Google 20+ role** — chưa nộp cái nào, khuyến nghị xin referral trước (Harry từng intern Google Chrome)

### 2c. ~40 công ty chưa quét Playwright (xem mục 1c)

---

## 3. Quy tắc bảo mật/vận hành BẮT BUỘC (giữ nguyên từ trước, vẫn đúng)

- **Email đúng:** `nguyenharry2662@gmail.com` — KHÔNG BAO GIỜ dùng `harry.nguyen@timoto.ai` hay `harrynguyenswe@gmail.com` (email cũ sai, đã sửa trong `cv.md` 2026-07-10)
- **KHÔNG BAO GIỜ nói** "sole engineer" — Harry là "primary engineer on 3-person team" tại TiMoto
- Apply cho mọi role bất kể visa/kinh nghiệm/năm tốt nghiệp — chỉ cảnh báo, không chặn
- Generate LaTeX CV khi score ≥ 3.0; compile bằng `node generate-latex.mjs {in}.tex {out}.pdf` (KHÔNG BAO GIỜ gọi `tectonic` trực tiếp — script này tự động strip `\pdfgentounicode=1` và `\input{glyphtounicode}` trước khi compile; gọi tectonic thẳng sẽ lỗi "Undefined control sequence" dù pipeline thật vẫn chạy tốt)
- **KHÔNG BAO GIỜ bịa số liệu** trong CV/report dù user có nói "cứ ghi đại rồi sửa sau" — luôn hỏi lại hoặc để trống, chỉ ghi số khi user xác nhận đã verify thật (xem case 68% MTTI — Harry tự verify với leadership rồi mới cho vào)
- **KHÔNG BAO GIỜ submit application thay Harry** — luôn dừng trước nút Submit
- **KHÔNG BAO GIỜ tạo entry trực tiếp trong `applications.md`** — viết TSV vào `batch/tracker-additions/` rồi chạy `node merge-tracker.mjs`
- Update status qua `node set-status.mjs <report#|company> <State> [--note]` — giờ đã hoạt động (đã restore ở mục 1a)

---

## 4. GitHub / gh CLI — lưu ý quan trọng

Máy này có **2 tài khoản GitHub đã login qua `gh auth`**: `HarryNguyenTimoto` (mặc định active) và `HarryNguyen2662` (chủ sở hữu remote `harry` — fork cá nhân của Harry tại `HarryNguyen2662/career-ops.git`). Nếu push/PR vào remote `harry` bị lỗi 403 "Permission denied to HarryNguyenTimoto", chạy `gh auth switch --user HarryNguyen2662` trước khi thử lại.

Remote setup hiện tại:
- `origin` → `santifer/career-ops.git` (upstream OSS gốc, có branch protection thật)
- `harry` → `HarryNguyen2662/career-ops.git` (fork cá nhân của Harry, `main` local track remote này)

PR #9 (session này) đã merge vào `harry/main` — không có branch protection chặn merge trên fork cá nhân này (khác upstream).

---

## 5. Vị trí file quan trọng

- CV gốc: `cv.md` (project root, LaTeX trực tiếp — KHÔNG qua `templates/cv-template.tex`)
- CV tailor theo job: dùng `build-cv-latex.mjs {json} {out}.tex` rồi `generate-latex.mjs` (qua `templates/cv-template.tex`)
- Profile: `config/profile.yml`
- Tracker: `data/applications.md` (155 dòng, xem mục 1b)
- Pipeline/inbox: `data/pipeline.md` (985 dòng đang chờ)
- Story bank: `interview-prep/story-bank.md` (nguồn STAR+R stories — cùng độ tin cậy với `cv.md`)
- Reports: `reports/{num}-{slug}-{date}.md` (155+ file, max hiện tại #157)
- CV output: `output/{num}-{slug}-{name}-{date}.tex/.pdf`, master resume ở `output/master/`
- Web app: `web/` (v0.3.0, cần `CAREER_OPS_ROOT` env var nếu chạy từ nơi khác main worktree)
